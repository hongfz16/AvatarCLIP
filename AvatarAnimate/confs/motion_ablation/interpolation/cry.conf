general {
    base_exp_dir = ./exp/motion_ablation/interpolation/cry
    mode = motion
    text = a rendered 3d man is drinking water
}

pose_generator {
    type = VPoserCodebook
}

motion_generator {
    type = MotionInterpolation
}